import 'package:flutter/material.dart';

class SitePlanBlank extends StatelessWidget {
  const SitePlanBlank({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}